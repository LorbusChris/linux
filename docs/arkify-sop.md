# SOP: rebasing and re-arkifying a downstream kernel branch

This document now lives in the repository that implements it:

**https://github.com/LorbusChris/arkify-automation/blob/main/docs/arkify-sop.md**

It used to be kept byte-identical on each `arkify-local-infra-*` branch here.
Three hand-synced copies drifted in practice, and the procedure is process
documentation rather than kernel source, so it belongs next to the scripts
that run it — see §9 of the SOP for the automation that performs §2–§3 on a
daily cron.
